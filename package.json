{
  "name": "weel-keypad",
  "version": "0.0.1",
  "description": "A Virtual Number Keypad.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run build:dev",
    "build:dev": "cross-env NODE_ENV=development rollup -c"
  },
  "author": "wallen",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "cross-env": "^5.1.3",
    "rollup": "^0.54.1"
  }
}
